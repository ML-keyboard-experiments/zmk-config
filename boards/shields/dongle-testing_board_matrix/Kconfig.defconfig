if SHIELD_DONGLE_TESTING_BOARD_MATRIX

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Test board m"

config ZMK_SPLIT
    default y

config ZMK_SPLIT_ROLE_CENTRAL
    default n

endif